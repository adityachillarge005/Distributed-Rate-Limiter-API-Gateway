# Distributed-Rate-Limiter-API-Gateway
A Django REST API Gateway with JWT authentication, Redis-based distributed rate limiting, Token Bucket and Sliding Window Log algorithms, atomic Lua scripts, asynchronous request processing, and round-robin load balancing.
